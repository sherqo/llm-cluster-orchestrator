# Proto

what's this? this is for gRpc proto files, which are used to define the structure of the messages that will be sent between:

- the master and the agents: `agent.proto`
- the master and the workers: `worker.proto`
